# Dotfiles

To Install: 
```
git --work-tree=$HOME --git-dir=$HOME/.files.git init && \
git --work-tree=$HOME --git-dir=$HOME/.files.git remote add origin https://github.com/DanielTomlinson/Dotfiles.git && \
git --work-tree=$HOME --git-dir=$HOME/.files.git pull origin master && \
make osx
```
